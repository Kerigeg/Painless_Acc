# 实际验证记录

日期：2026-09-19。环境：macOS、Node 26.7、已安装的 Google Chrome。

## 已执行

| 检查                     | 实际结果                                                                                    |
| ------------------------ | ------------------------------------------------------------------------------------------- |
| TypeScript 严格类型检查  | 通过，`npm run build` 内运行 `tsc --noEmit`                                                 |
| Vite 生产构建            | 通过，产物为 `dist/`                                                                        |
| 独立规则／数学／校验测试 | `npm test`：29 项通过                                                                       |
| 浏览器流程               | `npm run test:e2e`：10 项通过（5 个流程 × 桌面／手机）                                      |
| 桌面布局                 | 1440×1000，实际生成并查看首页、行动计划、UID 导入和月感电专题截图                           |
| 手机布局                 | 390×844，实际生成并查看首页、行动计划、UID 导入和月感电专题截图；关键页面没有页面级横向溢出 |
| 浏览器运行异常           | 空白账号完整流程未捕获 pageerror                                                            |
| 依赖安装检查             | npm 安装完成，报告 0 vulnerabilities；不等于完整安全审计                                    |

## 自动化覆盖

1. 空白账号填写目标症状 → 添加凯亚及当前队伍 → 诊断 → 展开基础教学 → 补可用状态 → 查看具体教程。
2. 保存固定场景与基线 → 完成前置 → 提交“操作不出来” → 下一次任务实际变为简化步骤。
3. 刷新保留角色资料；导出 JSON；错误 JSON 被拒绝；有效导出重新导入恢复。
4. 超范围等级输入显示错误；未知等级和圣遗物字段保留为 null，不触发确定性投入判断。
5. 示例芭芭拉路径 → 前置记录 → “现在已经够用了” → 本阶段结束。
6. 数学边界：暴击率 0／50／100／超100、非法数值；抗性负值／0／75%分界／100%；固定回能不乘充能；缺口非负；未知库存不变为零。
7. 队伍仅使用已录入角色；待培养角色、未知解锁、缺前置不能判定可开始；任务有零树脂预算与停止条件。
8. 材料缺 1 摩拉时阻止升级；材料和前置均满足才放行；战技从1改为2后升级任务消失，历史保留。
9. 重复装备 ID、非法主副词条、星级强化上限、未拥有队员被拒绝。
10. 档案变化后完成状态需要复核；旧历史不删除；“完成没有改善”使相关结论转为待补充，最近反馈优先。

11. UID 数据转换：真实字段名、数值单位、武器精炼、圣遗物等级、天赋额外等级；未知数据保留、重复合并不重复创建、不同 UID 拒绝混合、名称映射恢复维持身份。
12. UID 浏览器流程使用明确的测试夹具：格式错误、公开预览、选择合并、缓存刷新、保存恢复、导入前后快照；私密展柜和限流不改动档案。
13. 月感电流程：逐个确认拥有与可用状态 → 应用四人模板 → 补充实战观察 → 诊断 → 前置记录 → 具体教程 → 无改善反馈触发重新诊断。未拥有或未就绪角色不能应用模板。
14. 服务端验证：TTL 缓存与并发合并、无效 UID、上游错误和限流；生产 Node 服务实际启动，首页 200、非法 UID 400、POST 405、不存在页面 404。
15. 真实接口验证：使用 Enka 文档公开示例 UID 读取 4 位角色、20 件圣遗物；独立浏览器完成预览、合并、刷新恢复与历史保存。一件武器缺少名称映射，如实显示未知；没有用模拟数据替代真实接口结果。

截图与导出样本位于 `test-results/`（本地生成，不纳入版本控制）：

- `desktop-home.png` / `mobile-home.png`
- `desktop-plan.png` / `mobile-plan.png`
- `desktop-lunar.png` / `mobile-lunar.png`
- `desktop-uid.png` / `mobile-uid.png`（测试夹具）
- `desktop-export.json` / `mobile-export.json`

## 没有执行、不能宣称通过

- 原神客户端内的实战验证、伤害实测、材料领取或账号访问。
- 所有角色、当前版本全部机制、全仓库装备最优解。
- 真实 iOS／Android 硬件和 Safari／Firefox；手机为 Chrome 视口及触摸模拟。
- 公网部署、远程 GitHub 推送、跨设备持久化。
- 固定坐标的材料采集路线、随机掉落概率或刷取天数。
- P2 完整账号／仓库导入、战斗模拟与 P3 自动发现／更新系统。公开展柜 UID 导入已实现并实测。

首版资料核实与网站行为测试不能代替游戏内策略验证。页面和 README 均明确此边界。

## 新角色名称修复补充

- 严格类型检查与生产构建通过；当前单元测试共 31 项通过。
- 新增回归覆盖：用户校正名称在重新导入后保留；奥黛塔与阿罗夏的用户确认 ID 映射；旧档案刷新修复名称，装备不变、历史不改，未知天赋仍为 null。
- 名称 ID 对应来自本次用户反馈，未宣称已由上游游戏数据独立验证；专属机制未实现。
